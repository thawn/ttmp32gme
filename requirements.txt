# Core dependencies
flask>=3.0.0
werkzeug>=3.0.0
packaging>=23.0
mutagen>=1.47.0  # For audio tag reading (replaces Music::Tag)
pillow>=10.0.0  # For image handling
requests>=2.31.0

# Database
# sqlite3 is included in Python standard library

# Testing
pytest>=7.4.0
pytest-html>=4.0.0

# Optional: for async support if needed
# aiofiles>=23.0.0
# aiohttp>=3.9.0
